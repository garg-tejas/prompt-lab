# For dev, leave this empty — Vite proxy handles /api routing
# For production, set to your API base URL if different from current domain
VITE_API_URL=
VITE_CLERK_PUBLISHABLE_KEY=pk_test_YOUR_CLERK_PUBLISHABLE_KEY
